#include <stdio.h>
#include <stdlib.h>
#include "afnd.h"

AFND *AFNDIntermedio(AFND *afnd) {
    if( !afnd ) return NULL;
    
    afnd* interm = NULL;
    // Detectamos varios estados simultáneos en el afnd y creamos uno
    // nuevo como la unión de los estados en los que podría estar a la
    // vez.
      
    
    
    
}

